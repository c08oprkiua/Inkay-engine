#include "profile.hpp"

/*
Basic idea of usedfields: Use to build an exact queue (array) of what is going 
to be patched, then send that off in a batch install to the installer function.
This makes it so the patcher only patches what we want, and also patches less.
*/
void Profile::SetToStock(){

}

void Profile::SetToDefault(){

}

void Profile::GenerateUsedFields(){

}